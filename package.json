{
  "name": "vectordrift-site",
  "private": true,
  "version": "0.0.0",
  "description": "Vector Drift terminal site (static). Tests cover the intent layer.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
